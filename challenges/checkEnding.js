// Takes two strings as input and checks whether the contents of the second
// string matches the end of the first string.

// Uses the endsWith method to compare str1 to str2.


function checkEnding(str1, str2) {
  return str1.endsWith(str2);
}

// checkEnding("abc", "bc") returns true.
