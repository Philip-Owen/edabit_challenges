// Takes a string as input and returns a number based on how many words are in
// the string.

// Splits the string into an array seperating

function countWords(str){
	return str.split(' ').length;
}

// countWords("It's high noon") would return 3
