
// Takes a single number as input and adds up all of the numbers from 1 to the 
// number passed in.
// If num = 4, it adds 1 + 2 + 3 + 4.
// Uses formual of "N(N + 1)/2"


function addUp(num) {
  return num * ((num + 1) / 2);
}


// addUp(4); would return 10.
